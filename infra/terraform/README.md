# Terraform

This folder holds the shared deployment inputs for the Vercel microfrontends and the Kubernetes backend stack.

Current scope:

- model the website/admin microfrontend roots
- model the backend container images and ports
- provide a single place for environment-specific variables

The Terraform scaffold is intentionally minimal until a specific cloud target is chosen.
