# Shared Packages

This directory holds shared code used by the web, mobile, and service layers.

- `shared-ui`: cross-platform UI building blocks
- `shared-api`: typed contracts and generated clients
- `shared-types`: workspace-wide TypeScript types
- `shared-config`: reusable configuration files
- `shared-validation`: shared Zod schemas