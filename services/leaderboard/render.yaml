services:
  - type: web
    name: code-quest-leaderboard
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 8085
      - key: SERVICE_MODE
        value: db
